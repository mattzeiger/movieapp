var mysql = require('../mysql');
var db = null;
module.exports = function () {
    if(!db) {
		var db = mysql.createConnection(
		    {
		      host     : 'localhost',
		      user     : 'root',
		      password : 'root',
		      database : 'movieapp',
		    }
		);
    }
    return db;
};
